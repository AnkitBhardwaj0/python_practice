#Reverse the order of the words, but do not reverse the letters inside each word.
sentence="Reverse the order of the words, but do not reverse the letters inside each word"
print(" ".join(sentence.split()[::-1]))
    